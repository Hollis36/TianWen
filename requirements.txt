# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
pytorch-lightning>=2.0.0

# Configuration
hydra-core>=1.3.0
omegaconf>=2.3.0

# Models - Core
transformers>=4.45.0
ultralytics>=8.3.0

# Models - RF-DETR (SOTA detector)
# Install separately: pip install rfdetr
# Or install via optional dependency: pip install -e ".[rfdetr]"
rfdetr>=0.1.0

# Models - Grounding-DINO (open-vocabulary detection)
# groundingdino  # Install from: pip install groundingdino
# OR use autodistill wrapper:
# autodistill-grounding-dino

# Data processing
pycocotools>=2.0.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
albumentations>=1.3.0

# Logging & Visualization
tqdm>=4.65.0
wandb>=0.15.0
tensorboard>=2.14.0
rich>=13.0.0

# Optional: Flash Attention (recommended for VLMs)
# flash-attn>=2.5.0

# Optional: Distributed training
# deepspeed>=0.10.0

# Development
pytest>=7.0.0
black>=23.0.0
isort>=5.12.0
